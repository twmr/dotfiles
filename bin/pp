#!/usr/bin/env python3

import sys

if __name__ == "__main__":
   if len(sys.argv) < 2:
      print("empty")
      exit()
   input = sys.argv[1]
   sp = input.split(':')
   for s in sp:
      print(s)
